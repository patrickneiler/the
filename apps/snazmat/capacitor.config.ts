import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'snazmat',
  webDir: '../../dist/apps/snazmat',
  bundledWebRuntime: false,
};

export default config;
