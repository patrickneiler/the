import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tourbo',
  webDir: '../../dist/apps/tourbo',
  bundledWebRuntime: false,
};

export default config;
